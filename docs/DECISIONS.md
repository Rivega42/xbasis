# Architecture Decision Records

> История архитектурных решений проекта xbasis

---

## ADR-001: Выбор стека

**Дата:** 2025-01-20
**Статус:** Accepted

### Контекст
Нужно выбрать технологии для MVP платформы.

### Решение
- **Frontend:** Next.js 14 + React + Tailwind + shadcn/ui
- **Backend:** FastAPI (Python)
- **Database:** PostgreSQL (Supabase)
- **AI:** Claude API + OpenAI (fallback)

### Причины
- Next.js: SSR, отличный DX, большое сообщество
- FastAPI: async, автодокументация, типизация
- PostgreSQL: надёжность, Supabase даёт managed решение

---

## ADR-002: JWT Auth vs Sessions

**Дата:** 2025-01-20
**Статус:** Accepted

### Контекст
Выбор механизма аутентификации.

### Решение
JWT с refresh tokens.

### Причины
- Stateless (масштабирование)
- Работает с мобильными приложениями
- Refresh tokens для безопасности

---

## ADR-003: Paddle vs Stripe

**Дата:** 2025-01-21
**Статус:** Accepted

### Контекст
Выбор платёжной системы.

### Решение
Paddle как Merchant of Record.

### Причины
- Paddle берёт на себя налоги
- Работает с РФ/СНГ
- Проще compliance

---

## ADR-004: ShipKit Documentation System

**Дата:** 2025-01-22
**Статус:** Accepted

### Контекст
Нужна система документации для AI-assisted разработки.

### Решение
ShipKit методология:
- `CLAUDE.md` — entry point
- `docs/CONTEXT.md` — что за проект
- `docs/FEATURES.md` — статус функций
- `docs/SESSION.md` — текущая сессия
- `docs/DECISIONS.md` — ADR
- `docs/PROBLEMS.md` — известные проблемы

### Причины
- Сохранение контекста между сессиями
- Структурированная работа с AI
- История решений

---

## ADR-005: Claude Code Agent System

**Дата:** 2026-01-27
**Статус:** Accepted
**Complexity:** M

### Контекст
Нужна система для эффективной AI-assisted разработки с автоматизацией.

### Решение
Иерархия агентов в `.claude/`:

```
@architect (стратегический)
     ↓
@orchestrator (тактический)
     ↓
@backend, @frontend, @database, @ai,
@devops, @qa, @docs, @reviewer
```

### Компоненты
- `.claude/agents/` — 10 специализированных агентов
- `.claude/skills/` — переиспользуемые знания
- `.claude/settings.json` — hooks для автоформатирования

### Роли
| Агент | Уровень | Роль |
|-------|---------|------|
| architect | Стратегический | Планирование, ADR, консультации |
| orchestrator | Тактический | Координация, выполнение |
| остальные | Исполнительный | Специализированные задачи |

### Причины
- Разделение ответственности
- Автоматическая оркестрация
- Масштабируемость команды агентов

---

## ADR-006: AI Skills System for Users

**Дата:** 2026-01-27
**Статус:** Proposed
**Complexity:** L

### Контекст
Пользователи xbasis должны "приходить и творить, а не настраивать". Нужна система которая автоматически предоставляет AI правильный контекст для генерации кода.

### Решение
Система Skills + Context Builder + Hooks в `src/api/ai/`:

```
src/api/ai/
├── skills/
│   ├── loader.py           # Загрузка skills по типу проекта
│   └── templates/          # Markdown файлы с правилами
├── context/
│   └── builder.py          # Сборка контекста для AI
└── hooks/
    ├── pre_hooks.py        # Валидация до генерации
    └── post_hooks.py       # Lint/format после
```

### Режимы работы (как в Claude Code / Replit)

```
┌─────────────────────────────────────────┐
│         xbasis AI Gateway               │
│                                         │
│   [💬 Chat]         [📋 Plan]           │
│                                         │
│   Сразу отвечает    Сначала:            │
│   и генерирует      1. Анализ задачи    │
│   код               2. План действий    │
│                     3. Подтверждение    │
│                     Потом: генерация    │
└─────────────────────────────────────────┘
```

### Как работает
1. Пользователь создаёт проект (выбирает шаблон)
2. При каждом запросе к AI:
   - Context Builder собирает: project info + skills + history
   - Pre-hooks валидируют запрос
   - AI генерирует код
   - Post-hooks форматируют и проверяют

### Project Templates
| Шаблон | Skills | Описание |
|--------|--------|----------|
| SaaS | nextjs, auth, db, api | Полноценное SaaS приложение |
| Landing | nextjs, seo, forms | Лендинг с формами |
| API | fastapi, db, auth | Backend API |

### Преимущества
- Пользователь не настраивает AI
- Консистентный код
- Автоматическая валидация
- Расширяемость через новые skills

### Риски
| Риск | Митигация |
|------|----------|
| Слишком большой контекст | Лимит токенов, summarization |
| Skills устаревают | Версионирование, обновления |
| Конфликты между skills | Приоритеты, тестирование |

### План реализации
1. Skills Loader (2-3 часа)
2. Context Builder (2-3 часа)
3. Hooks System (2 часа)
4. Project Templates (2 часа)

**Estimated:** 8-10 часов

---

## ADR-007: Multi-Agent Frameworks (CrewAI / LangGraph)

**Дата:** 2026-01-27
**Статус:** Under Consideration ⏸️
**Complexity:** XL

### Контекст
Вопрос: нужны ли мульти-агентные фреймворки (CrewAI, AutoGen, LangGraph) для xbasis?

### Что это такое
Мульти-агентные фреймворки запускают **несколько LLM параллельно**, каждый со своей ролью:

```
                    ┌─────────────┐
                    │ Orchestrator│
                    │    (LLM)    │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│  Backend    │    │  Frontend   │    │  Database   │
│   Agent     │    │   Agent     │    │   Agent     │
│   (LLM)     │    │   (LLM)     │    │   (LLM)     │
└─────────────┘    └─────────────┘    └─────────────┘
      │                  │                  │
      ▼                  ▼                  ▼
   api.py           page.tsx           schema.sql
```

**Ключевое:** каждый агент = отдельный вызов LLM API

### Когда это РЕАЛЬНО нужно

| Сценарий | Нужны мульти-агенты? | Почему |
|----------|---------------------|--------|
| Чат с AI, пошаговая работа | ❌ Нет | Один Claude справится |
| "Добавь кнопку" | ❌ Нет | Простая задача |
| Plan mode (сначала план) | ❌ Нет | Один Claude с промптом |
| Генерация целого проекта | 🤔 Возможно | Много файлов параллельно |
| Автономный агент (работает часами) | ✅ Да | Сложная координация |
| Агенты спорят/ревьюят друг друга | ✅ Да | Разные "точки зрения" |
| Research агент (поиск + анализ) | ✅ Да | Разные источники параллельно |

### Реальные примеры использования

**1. Devin / Cursor Background Agent**
```
Задача: "Пофикси все баги в репозитории"
Агенты:
- Analyzer: находит баги
- Fixer: пишет фиксы  
- Tester: проверяет фиксы
- Reviewer: проверяет качество
Работает часами автономно.
```

**2. AI Software Company (CrewAI пример)**
```
Задача: "Создай стартап для доставки еды"
Агенты:
- CEO: общая стратегия
- CTO: техническая архитектура
- Developer: пишет код
- Designer: UI/UX
Каждый со своей "личностью" и целями.
```

**3. Research Assistant**
```
Задача: "Исследуй рынок AI-стартапов"
Агенты:
- Searcher: ищет информацию
- Analyzer: анализирует данные
- Writer: пишет отчёт
- Critic: проверяет факты
```

### Сравнение с Plan Mode

| | Plan Mode (один Claude) | Multi-Agent (CrewAI) |
|---|---|---|
| Вызовов API | 1-2 | 5-20 |
| Стоимость | $0.02-0.10 | $0.50-5.00 |
| Время | 5-30 сек | 1-10 минут |
| Параллельность | ❌ | ✅ |
| Разные "точки зрения" | ❌ | ✅ |
| Автономная работа | ❌ | ✅ |
| Сложность реализации | Низкая | Высокая |

### Вывод для xbasis

**MVP (сейчас):** Plan Mode достаточно
- Пользователь работает пошагово
- Один Claude с правильным контекстом
- Дёшево и быстро

**Будущее (возможно):** Multi-Agent для:
- "Создай мне полный проект за 10 минут"
- Автономная работа в фоне
- Premium фича (дороже для пользователя)

### Решение
⏸️ **Отложено.** Сначала реализуем Skills System + Plan Mode. 
Вернёмся к мульти-агентам если:
1. Пользователи попросят "генерацию всего проекта"
2. Появится запрос на автономную работу
3. Plan Mode окажется недостаточным

### Фреймворки на заметку
| Фреймворк | Плюсы | Минусы |
|-----------|-------|--------|
| **CrewAI** | Простой, роли, популярный | Меньше контроля |
| **LangGraph** | Полный контроль, графы | Сложнее |
| **AutoGen** | Microsoft, мощный | Переусложнён |

---

## ADR-008: Claude Code Tasks для персистентности

**Дата:** 2026-01-29
**Статус:** Accepted
**Complexity:** S

### Контекст
Проблема "амнезии агента": закрыл терминал — Claude Code забыл все задачи.
Раньше использовали `docs/SESSION.md` для ручного трекинга.

### Решение
Использовать встроенную систему Tasks с фиксированным ID:

```bash
CLAUDE_CODE_TASK_LIST_ID=xbasis claude
```

### Как работает

#### Хранение
```
~/.claude/
└── tasks/
    └── xbasis.json    # Задачи сохраняются на диске
```

#### Инструменты агента
```
TaskCreate   — создать задачу с описанием и зависимостями
TaskList     — показать все задачи
TaskGet      — получить детали задачи
TaskUpdate   — обновить статус, добавить зависимости
```

#### Зависимости между задачами
```
1. [completed] Setup project structure
2. [in_progress] Create database schema
3. [pending] Implement API (blocked by #2)    ← ждёт #2
4. [pending] Add tests (blocked by #3)        ← ждёт #3
```

### Интеграция с нашими агентами

```
@architect создаёт план
        ↓
    TaskCreate (задачи с зависимостями)
        ↓
@orchestrator берёт задачу
        ↓
    TaskUpdate (status: in_progress)
        ↓
@backend/@frontend выполняет
        ↓
    TaskUpdate (status: completed)
        ↓
Следующая задача разблокируется автоматически
```

### Паттерн Worker + Checker

```bash
# Терминал 1 — Worker
CLAUDE_CODE_TASK_LIST_ID=xbasis claude
> Возьми следующую задачу и выполни

# Терминал 2 — Checker  
CLAUDE_CODE_TASK_LIST_ID=xbasis claude
> Проверяй completed задачи, запускай тесты
> Если тесты падают — создай задачу с багом
```

Оба терминала видят один список. Синхронизация мгновенная.

### Сравнение

| | Раньше (SESSION.md) | Теперь (Tasks) |
|---|---|---|
| Хранение | Ручное в markdown | Автоматическое |
| Персистентность | Если не забыл записать | Всегда |
| Зависимости | Нет | Встроенные |
| Параллельность | Нет | Worker + Checker |
| Субагенты | Не видят | Shared task list |

### Ограничения

- Задачи в `~/.claude/`, не в проекте (нельзя закоммитить)
- Нет поиска по истории закрытых задач
- Для git-backed истории — использовать Beads

### Workflow

```bash
# Начало работы
CLAUDE_CODE_TASK_LIST_ID=xbasis claude

# Посмотреть задачи
> Покажи все задачи

# Продолжить с прошлой сессии
> Какие задачи остались? Возьми следующую.

# Конец сессии — задачи автоматически сохранены
```

### Причины
- Нативная интеграция в Claude Code (v2.1.16+)
- Персистентность без усилий
- Параллельная работа для сложных задач
- Зависимости понимаются агентом

### Источник
- [Habr: Claude Code научился запоминать задачи](https://habr.com/ru/articles/989106/)

---

## Шаблон для новых ADR

```markdown
## ADR-XXX: Название

**Дата:** YYYY-MM-DD
**Статус:** Proposed | Accepted | Deprecated | Under Consideration
**Complexity:** S | M | L | XL

### Контекст
Почему нужно это решение?

### Решение
Что решили делать?

### Альтернативы
- Вариант A: плюсы/минусы
- Вариант B: плюсы/минусы

### Последствия
- Позитивные: ...
- Негативные: ...
```
